import { TargetClassMetadata } from "@src/metadata/storage/definitions/common";

export default interface RawResolverMetadata extends TargetClassMetadata {
  // TODO: maybe more fields in the future, like "namespace" or sth
}
